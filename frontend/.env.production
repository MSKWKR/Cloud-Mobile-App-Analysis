# Production build overrides (loaded by `vite build`, on top of .env).
# Empty = same-origin relative requests (e.g. fetch("/upload")), proxied by nginx.
# Firebase VITE_* values are inherited from .env.
VITE_BACKEND_URL=
